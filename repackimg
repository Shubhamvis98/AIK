#!/bin/bash

source bin/funcs.sh
chkarch
banner
RPWD=`pwd`
IMG=`cat $RPWD/split/img_name`

if [[ -d $RPWD/ramdisk ]]
then
	cd $RPWD/ramdisk
	find . | cpio -H newc -o > $RPWD/split/ramdisk.cpio
fi

cd $RPWD/split
$MAGISKBOOT repack $IMG
mv new-boot.img ..

